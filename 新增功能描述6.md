# 新增功能：窗口自适应多终端布局
## 原有基础交互功能
点击图标同时触发5项原有交互：
1. 切换文字字体颜色
2. 切换条目背景色
3. 显示/隐藏应用描述文字
4. 切换图标尺寸大小
5. 弹出Toast提示当前图标名称

## 新增功能名称
窗口尺寸监听，自动区分大屏/小屏两套布局，多设备兼容适配

## 新增功能描述
组件加载时自动获取窗口宽度，监听窗口缩放、横竖屏、分屏、设备流转尺寸变化；以600vp为阈值区分大屏、小屏，分别使用两套图标尺寸与布局；增加窗口获取失败兜底逻辑，兼容异常场景，适配手机、平板、折叠屏全设备。

## 实现方式
1. 使用window API获取当前窗口宽度，转换vp单位存储状态变量
2. 注册windowSizeChange事件实时监听窗口尺寸变化
3. if分支判断宽度渲染两套独立Row布局，差异化图标尺寸
4. catch捕获窗口API异常，默认赋值手机宽度360vp兜底防布局错乱
5. 复用原有点击事件，不新增交互按钮，无额外第三方依赖

## 功能效果
1. 手机竖屏（<600vp）：小尺寸图标布局
2. 平板/横屏/宽分屏（≥600vp）：更大图标布局
3. 旋转屏幕、拖动分屏时布局实时切换
4. API异常时页面不会崩溃，正常展示手机布局
5. 保留原有全部5项点击交互效果