新增功能：窗口大小自适应（适配横竖屏、分屏、跨设备自由流转）
功能名称
应用页面动态大小屏自适应布局
功能描述
在原有全部 5 项点击交互功能不改动、完整保留的基础上，新增窗口尺寸自动适配能力。应用会实时监听当前窗口宽度，根据屏幕宽窄自动切换两套布局；手机竖屏、窄分屏使用紧凑小屏布局，手机横屏、平板、宽分屏使用宽松大屏布局，旋转屏幕、拖动分屏分割线、跨设备自由流转时页面布局会无感实时切换。
实现方式
新增状态变量存储当前窗口宽度，驱动布局自动更新
页面加载时获取初始窗口尺寸，同时监听窗口尺寸变化事件
通过判断窗口宽度阈值区分大小屏，两套布局独立编写
图标放大尺寸区分适配：小屏放大上限 55vp，大屏放大上限 72vp
配套 module.json5 开启 continuable 流转配置，流转后状态与布局同步生效
仅使用系统基础窗口 API，兼容主流鸿蒙版本，修复常量报错无编译错误
功能效果
竖屏小屏状态：紧凑横向布局，点击图标最大放大至 55vp
横屏 / 平板大屏状态：宽松横向布局，点击图标最大放大至 72vp
分屏拖动窗口宽窄时，布局与图标尺寸上限实时自动切换
应用跨设备自由流转后，原有文字颜色、背景、图标大小、描述显隐状态全部保留，并自动适配目标设备大屏布局
原有 5 项点击交互（Toast 提示、文字换色、背景换色、描述显隐、图标缩放）全部完整保留不受影响