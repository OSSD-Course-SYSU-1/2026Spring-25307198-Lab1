# 图标自适应背景色工程 - 工程文件描述
## 一、工程概述
本工程基于HarmonyOS ArkTS开发，实现图标主色自动提取与自适应背景色生成功能。通过读取图标像素数据，统计出现频率最高的颜色作为主色，并自动生成匹配、柔和的背景色，提升界面视觉效果。

## 二、工程目录结构
entry/src/main/ets├── constants│ └── CommonConstants.ets # 全局常量定义（图片尺寸、颜色阈值）├── entryability│ └── EntryAbility.ets # 应用入口，管理生命周期├── pages│ ├── IconItem.ets # 单个图标组件，展示带背景色的图标│ └── Index.ets # 主页面，展示图标列表├── utils│ ├── ColorUtils.ets # 颜色处理：主色统计、背景色生成│ ├── ImageUtils.ets # 图片处理：像素读取、转像素图│ ├── Logger.ets # 日志输出工具│ └── WindowUtil.ets # 窗口尺寸适配工具└── viewmodel├── AppInfoModel.ets # 应用信息数据结构└── MockDataModel.ets # 测试用模拟图标数据

## 三、核心文件作用
1. **EntryAbility.ets**：应用启动入口，负责程序初始化与生命周期管理。
2. **Index.ets**：应用首页，加载图标列表，调用组件展示。
3. **IconItem.ets**：单个图标条目，绑定图标与自适应背景色。
4. **ColorUtils.ets**：核心工具，统计主色、生成背景颜色。
5. **ImageUtils.ets**：读取图片，转换为像素数据供颜色计算。
6. **CommonConstants.ets**：统一存放全局固定参数。
7. **MockDataModel.ets**：提供图标测试数据。

## 四、核心执行流程
1. 应用启动 → 加载首页 Index.ets
2. 首页渲染图标列表 → 调用 IconItem.ets
3. IconItem 调用 ImageUtils 读取图标像素
4. ImageUtils 返回像素数据 → 传给 ColorUtils
5. ColorUtils 统计主色 → 生成背景色
6. 背景色绑定到图标条目 → 完成界面展示

## 五、配置文件说明
1. **hvigorfile.ts**：鸿蒙工程构建配置，保证项目正常编译。
2. **OAT.xml**：开源合规审计配置，用于许可证检查与文件过滤。
3. **README.md / README_EN.md**：工程说明文档，包含功能介绍。

## 六、运行环境
- 开发工具：DevEco Studio
- 运行设备：HarmonyOS 手机、平板、模拟器

## 七、工程运行步骤
1. 使用 DevEco Studio 导入工程
2. 等待自动同步依赖
3. 连接模拟器或真机
4. 点击运行按钮，启动应用
5. 查看图标自动生成背景色效果

## 八、文件依赖关系
- Index.ets → IconItem.ets
- IconItem.ets → ImageUtils.ets、ColorUtils.ets
- ImageUtils/ColorUtils → CommonConstants.ets
- 首页数据来自 MockDataModel.ets

## 九、注意事项
- 支持图片格式：png、jpg
- 推荐使用方形图标，背景效果最佳
- 无需额外权限，直接运行